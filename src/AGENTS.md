# Purpose

React application composition, styling, content, and user-visible tests.

# Local Contracts

- Keep semantic labels, phone links, keyboard behavior, and reduced-motion fallback.
- Hero sources must match verified files in `public/assets/`.

# Verification

- `npm test -- --run src/App.test.tsx`
- `npm run build`

# Child DOX Index

- `components/AGENTS.md` — reusable page sections.
