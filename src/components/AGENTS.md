# Purpose

Reusable page sections and interaction components.

# Local Contracts

- Preserve component accessibility, labels, and existing business copy.
- Keep hero media ownership in `src/App.tsx` and `public/assets`.

# Verification

- Covered by `npm test -- --run src/App.test.tsx`.
